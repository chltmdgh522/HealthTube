extends base
include mixins/video


block content
  
    div.profile__data
        if !user.avatarUrl
            span 😊
        else 
            img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else
            h1.empty__message 영상이 없습니다.
