extends base.pug

block content 
    h4 영상을 편집하자 
    form(method="POST")
        input(name="title",placehodler="Video Title",value=video.title,required)
        input(value="Save", type="submit")
        