extends base.pug

block content 
    h4 영상 설명들을 편집해보세요!
    form(method="POST")
        input(name="title",placehodler="Video Title",value=video.title,required).submitnotinput
        input(placeholder="설명", required, type="text", name="description", minlength=10,value=video.description).submitnotinput
        input(placeholder="해시태그", required, type="text", name="hashtags",value=video.hashtags.join()).submitnotinput
        input(value="저장", type="submit").submitinput
        