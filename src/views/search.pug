extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeholder="찾는 제목을 입력하세요",name="keyword", type="text")
        input(type="submit", value="검색")

    each video in videos
        +video(video)